description: Request body for creating or updating a customer shipping contact.
content:
  application/json:
    schema:
      $ref: ../../schemas/customers/customer_shipping_contacts.yml
required: true